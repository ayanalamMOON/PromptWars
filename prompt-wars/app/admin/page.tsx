export default function AdminPage() {
    return (
        <main className="min-h-screen p-8">
            <h1 className="text-3xl font-bold text-primary">
                Game Master Console
            </h1>
            <p className="mt-4 text-gray-600">
                Wave control, match overview, system health
            </p>
            {/* WaveControlPanel, ProblemRevealBtn, PoisonPillTrigger, OverridePanel, SystemHealth, Check-In Board */}
        </main>
    );
}

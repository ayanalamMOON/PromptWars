# Ollama endpoint
OLLAMA_BASE_URL=http://127.0.0.1:11434

# Generation model (participants A/B)
GENERATION_MODEL=llama3:latest

# Judge model (used only for verdict scoring)
JUDGE_MODEL=llama3.2:3b

# Backward-compat fallback used by older code paths
FROZEN_MODEL=llama3:latest
